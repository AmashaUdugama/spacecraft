fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
pydantic==2.9.2
email-validator==2.2.0
pydantic-settings==2.5.2
python-dotenv==1.0.1

# ML / CV (real, lightweight, run every request)
opencv-python-headless==4.10.0.84
scikit-learn==1.5.2
numpy==1.26.4
pillow==10.4.0
joblib==1.4.2

# TensorFlow - required for the real CNN models (room classifier, style predictor).
# Large download (~500MB+). The app still runs and falls back to a heuristic
# automatically if this fails to install or the .h5 model files aren't trained yet,
# but for your proposal's tech stack to be genuinely used, install this.
tensorflow==2.21.0
